const myLibrary = [];
const addBook = document.getElementById("add");
const btnContainer = document.querySelector(".addBtnContainer")

function addBookToLibrary() {
    
}

addBook.addEventListener("click", () => {
    btnContainer.appendChild(123);
})